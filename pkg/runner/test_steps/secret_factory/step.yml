spec:
  inputs:
    secret_override:
      type: string
      default: ""
      sensitive: true
  outputs:
    secret:
      type: string
      sensitive: true
---
env:
  OUTPUT: ${{ inputs.secret_override }}
exec:
  command: [bash, -c, "echo 'secret=\"er\"' >${{ output_file }}"]
