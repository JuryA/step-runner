spec:
  inputs:
    echo:
      type: string
  outputs:
    echo:
      type: string
---
exec:
  command: [ bash, -c, "echo '{\"name\":\"echo\",\"value\":\"${{inputs.echo}}\"}' | tee ${{ output_file }}" ]
