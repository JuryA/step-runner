spec:
  inputs:
    name:
      type: string
    value:
      type: string
---
exec:
  command:
    - bash
    - -c
    - "echo '{\"name\":\"${{inputs.name}}\",\"value\":\"${{inputs.value}}\"}' >${{export_file}}"
