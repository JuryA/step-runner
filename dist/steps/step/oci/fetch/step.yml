spec:
  inputs:
    registry:
      type: string
    repository:
      type: string
    tag:
      type: string
    step_path:
      type: string
      default: ""
  outputs:
    fetched_step_path:
      type: string
    ref:
      type: string
---
exec:
  work_dir: ${{step_dir}}
  command:
    - "${{step_dir}}/run"
    - "--registry"
    - "${{inputs.registry}}"
    - "--repository"
    - "${{inputs.repository}}"
    - "--tag"
    - "${{inputs.tag}}"
    - "--step_path"
    - "${{inputs.step_path}}"
    - "--tag"
    - "${{inputs.tag}}"
    - "--output_file"
    - "${{output_file}}"
